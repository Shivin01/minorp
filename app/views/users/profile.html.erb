
  <center>
  <div class="box2">
    <div class="power">☮</div>
    <div class="userbox">
      <div class="pic">
        
      </div>
    </div>
    <p>Hi, <span class="user1"> 
    
    
        <strong>
          <%= current_user.email %>
        </strong>
      
</div>
</span></p>
</div>
<div class = "box3">
<div class="transitions-enabled" id="pins">
  <% @pins.each do |pin| %>
    <div class="box panel panel-default">
      <%= link_to (image_tag pin.image.url), pin %>
      <div class="panel-body">
        <h2>
          <%= link_to pin.title, pin %>
        </h2>
      </div>
      <%= link_to "Edit", edit_pin_path(@current_user, @pin), class: "btn btn-default" %>
  <%= link_to "Delete", pin_path(@current_user, @pin), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-default" %>


    </div>
  <% end %>
</div>

</div>

</center>


