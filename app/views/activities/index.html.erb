<h1> Friend's Activities </h1>

<% @activities.each do |activity| %>
	<%= 2 %>
	<div class = "activity">
	<%= link_to activity.owner.name, activity.owner if activity.owner %>
	added comment to <%= link_to activity.trackable.pin.title, activity.trackable.pin%>
	</div>
<% end %>